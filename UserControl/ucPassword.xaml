<UserControl x:Class="ucPassword"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400" FontSize="18" Padding="50" Background="#FFF9DD93">
    <StackPanel Background="#FFF9B116">
        <Label HorizontalAlignment="Center"  Content="{Binding prompttitle,FallbackValue='Enter Password:'}"></Label>
        <TextBox Text="{Binding prompttext}" Width="200"  Height="40"></TextBox>
        <Button Width="200"  Margin="10" Command="{Binding promptok}" IsDefault="True" Content="OK"/>
        <Button Width="200"  Margin="10" Command="{Binding promptcancel}" Content="Cancel"/>
    </StackPanel>
</UserControl>
